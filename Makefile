# Makefile — opt-in workflow targets.
#
# This file intentionally remains present even when no make-exposed
# workflow helpers are active. Build/test/lint commands continue to live
# in `test.sh` and the per-script entry points under `scripts/`.
