# Vector Siege Stage 1 Candidate

Implement Stage 1 from `TASK.md` in the frozen scaffold. Use the normal Codespace
tools and permissions available to you. Do not modify benchmark protocol files.

Run the scaffold build, unit tests, and browser journey. Use Playwright or Chrome
DevTools to validate the game through the browser, including localhost services
needed by those tools. The parent evaluator independently repeats the checks.

Leave all work in the candidate checkout for evaluator capture. Do not push or
open a pull request.

In the final response, report completion status, build, unit-test, and browser
results, wall-clock blocking events, human intervention, and any fan-out election
required by the arm instructions. Do not estimate monetary cost; the evaluator
records usage telemetry.
