# Phase 0C Preflight Fixture Prompt

This deterministic text fixture does not authorize or instruct paid execution.
For fixture validation, implement only the assigned node from the fixture graph. Honor its parent-owned
shared contract, predicted write set, dependency order, child validation, and
the 45-minute end-to-end arm cap. Emit only canonical Phase 0C task, status,
artifact, reclassification, or failure events. The transport adapter is not
agent-visible and must not change implementation behavior.

Do not create or update GitHub state directly. Do not retry a candidate defect.
The parent performs the same install, browser, unit, build, and Stage 1
Playwright evaluation for both treatments.
