{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "task-parallelism-phase-0c-canonical-event-fixture.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "events"],
  "properties": {
    "schema_version": {"const": "task-parallelism-phase-0c-canonical-event-fixture.v1"},
    "events": {"type": "array", "minItems": 1, "maxItems": 64, "items": {"type": "object", "additionalProperties": false, "required": ["event_id", "sequence", "task_id", "kind", "payload"], "properties": {"event_id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]{0,63}$"}, "sequence": {"type": "integer", "minimum": 1, "maximum": 64}, "task_id": {"type": "string", "minLength": 1, "maxLength": 64}, "kind": {"enum": ["task-assigned", "task-status", "artifact-published", "task-reclassified", "task-failed"]}, "payload": {"type": "object"}}}}
  }
}
