# Post-merge retro fix notes — 2026-06-14

## Skipped (out of scope for this pass)

### pr431-adr026-receipt-semantics

ADR-026 amendment for v26 session receipt semantics (`Load`, `In context`, `Receipt boundary`, stale replay) was deferred per retro pass scope: ADR edits are not applied in automated fix PRs. Proposed follow-up: amend `docs/decisions/adr-026-compliance-contracts.md` (or companion ADR) to record the 8-field handshake and two-column receipt contract introduced in PR #431.

## Applied

- **Load boundary (Option A):** After compaction without re-read, emit `## Session context receipt (stale)` with `Load: —` and `In context: no`; do not carry prior-boundary `Load: Read` into a new receipt boundary.
- **8-field handshake:** Aligned subagent bootstrap, multi-agent coordination, smoke Scenario A, and `046-process-discipline.sh` parity checks.
- **PR #432 session rotation:** Backfilled archive `2026-06-14_feature-session-receipt-in-context.md`; fixed `latest_summary.md` wording; added closeout rotation checklist to `process_pr_completion.md`.
