# Post-merge retro fix notes — 2026-06-15

## ADR updates (deferred — out of scope for fix job)

Per retro fix job policy, ADR file edits are not applied in this pass. Proposed follow-ups:

### ADR-030 — Mark PR #433 merged

- **Finding:** `docs/decisions/adr-030-non-blocking-review-pipeline.md` Implementation checklist still has open `- [ ] Merge PR #433`.
- **Action:** Check off the merge item and confirm stage-4 weekly review is on `main` (PR #433 merged 2026-06-15).

### ADR-031 — Ratify status

- **Finding:** `docs/decisions/adr-031-agent-model-roi-benchmark-policy.md` remains `Status: Proposed` after merge.
- **Action:** Accept ADR-031 (or document remaining Implementation items) now that the policy surface is on `main`.

## Context pack updates (addressed in code)

The following context-pack findings are satisfied by catalog-driven selection changes in this fix PR:

- **postmerge-retro:** `run-postmerge-retro.sh` now injects `docs/decisions/adr-027-opportunity-feedback-channel.md` after `select-context`.
- **pr-review:** `prompt_helpers.py` `PATH_TRIGGERED` now covers `^scripts/`, `^AGENTS.md$`, and `^\.github/prompts/`.

No new benchmark harness TSV packs were added (production routing uses `select-context`, not Stage 1E manifests).
