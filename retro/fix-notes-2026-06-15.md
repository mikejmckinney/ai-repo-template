# Post-merge retro fix notes — 2026-06-15

## ADR updates (applied in PR #440 follow-up)

### ADR-030 — Mark PR #433 merged

- Checked off Implementation item for PR #433 merge (2026-06-15).

### ADR-031 — Ratify status

- `Status` set to **Accepted**; open Implementation items (Class C, Stage 1E CP-2, overlay refresh) remain tracked in the ADR body.

## Context pack updates (addressed in code)

The following context-pack findings are satisfied by catalog-driven selection changes in this fix PR:

- **postmerge-retro:** `run-postmerge-retro.sh` now injects `docs/decisions/adr-027-opportunity-feedback-channel.md` after `select-context`.
- **pr-review:** `prompt_helpers.py` `PATH_TRIGGERED` now covers `^scripts/`, `^AGENTS.md$`, and `^\.github/prompts/`.

No new benchmark harness TSV packs were added (production routing uses `select-context`, not Stage 1E manifests).
