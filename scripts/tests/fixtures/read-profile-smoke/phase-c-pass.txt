Session handshake v25

| Agent | Composer |
| Role | OP |
| Model | composer-2.5 |
| AGENTS.md version | 25 |
| Session type | smoke |
| Dispatch mode | monolithic |
| Read profile | policy-adr |

## Session context receipt

- process_session_start.md — Read — re-loaded after compaction
- .context/rules/process_gates.md — Read — verify triggers

The summary was **wrong**. Trigger 1 is:

1. Issue references `.github/prompts/NN-*.md` (two-digit project prompt

The gate does not fire only when issues carry the bug label.
