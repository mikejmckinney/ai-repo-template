{
  "schema_version": "benchmark-subjective-grade.v1",
  "score_set_id": "stage-1d-canonical-v1",
  "eval_candidate_id": "eval-001",
  "grader_id": "cursor-llm-blind-v1",
  "grader_prompt_id": "model-roi-grader-v1",
  "rubric_id": "rubric.v1",
  "categories": {
    "correctness": {
      "subjective_points": 3,
      "max_subjective_points": 10,
      "rationale": "Helper skeleton covers allowlist loading, env defaults, GraphQL snapshotting, unresolved review-thread counting, SHA_CHANGED vs initial head, TIMEOUT, and RESULT=/HEAD= emission. Core polling is non-functional: the main while loop uses `local` outside any function (bash runtime error on first iteration). CONVERGED fires when any allow-listed review author exists with zero unresolved threads, but never checks non-pending review state or review commit OID against current head despite header semantics requiring terminal-on-head convergence. Participating bots are inferred from review authors only, not comments. Required `.github/prompts/pr-resolve-all.md` integration is absent, so agents cannot prefer the helper from the primary workflow surface.",
      "uncertain": false
    },
    "quality": {
      "subjective_points": 6,
      "max_subjective_points": 15,
      "rationale": "Script header and AI_REPO_GUIDE entries are thorough and repo-consistent in tone (set -uo pipefail, umask, REPO_ROOT, stderr diagnostics). Allow-list file is well commented. Deductions for maintainability: header contract text overstates implemented convergence semantics, inline GraphQL is monolithic, reviewThreads pagination cursor is unused, review-thread comment timestamps are fetched but omitted from quiet-window calculation, and the `local`-in-loop mistake signals the helper was not exercised beyond syntax lint.",
      "uncertain": false
    },
    "process": {
      "subjective_points": 2,
      "max_subjective_points": 5,
      "rationale": "Diff is focused (four files, no PLAN.md/AGENTS.md/state-surface churn) and AI_REPO_GUIDE plus scripts/checks/105-scripts.sh were updated. Process adherence is weak because a required deliverable\u2014`.github/prompts/pr-resolve-all.md` settle-window preference and documented fallback\u2014is missing from files-changed.txt, and the task-required shell-test/check wiring pattern (bats or dedicated check script) was not added despite explicit acceptance expectations.",
      "uncertain": false
    },
    "reliability": {
      "subjective_points": 1,
      "max_subjective_points": 5,
      "rationale": "Only minimal verification wiring: scripts/checks/105-scripts.sh adds bash -n/lint coverage. Objective test_wiring failed (0/2 patterns); no scripts/tests/pr-resolve-all-poll.bats or scripts/checks/*pr-resolve*poll* file. bats_poll_helper and full_test_sh were skipped. bash -n and lint-shell-conventions pass per objective evidence, but those cannot catch the runtime `local` failure or exercise CONVERGED/QUIET_ELAPSED/SHA_CHANGED contract paths; candidate.md reports no verification command output.",
      "uncertain": false
    }
  },
  "subjective_total": 12,
  "uncertainty_notes": [],
  "citations": [
    {
      "bundle_ref": "diff.patch",
      "claim": "Main while loop declares `local variables`, `local response`, and other `local` bindings outside any function, which causes bash to exit before polling can proceed."
    },
    {
      "bundle_ref": "diff.patch",
      "claim": "CONVERGED branch requires participating_bots>0 and unresolved_bot_threads==0 but does not inspect review.state or review.commit.oid against the current head SHA."
    },
    {
      "bundle_ref": "diff.patch",
      "claim": "Header comments state participating bots are allow-listed bots that reviewed or commented and terminal means a non-pending review on current head; implementation only iterates reviewAuthors from reviews.nodes."
    },
    {
      "bundle_ref": "diff.patch",
      "claim": "GraphQL reviewThreads query defines afterThreads pagination but the script never paginates beyond the first page."
    },
    {
      "bundle_ref": "diff.patch",
      "claim": "_parse_pr_state builds latestTimestamp from commit, review submittedAt, and PR comment updatedAt only\u2014not review-thread comment createdAt/updatedAt fields already present in the query."
    },
    {
      "bundle_ref": "diff.patch",
      "claim": "_emit_result prints RESULT=<value> and HEAD=<sha> with documented exit codes 0/2/3/4 for CONVERGED, QUIET_ELAPSED, TIMEOUT, SHA_CHANGED, and API_ERROR paths."
    },
    {
      "bundle_ref": "files-changed.txt",
      "claim": "Changed files are AI_REPO_GUIDE.md, scripts/checks/105-scripts.sh, scripts/lib/bot-allowlist.txt, and scripts/pr-resolve-all-poll.sh only\u2014no .github/prompts/pr-resolve-all.md or test/check wiring files."
    },
    {
      "bundle_ref": "candidate.md",
      "claim": "Task requires updating .github/prompts/pr-resolve-all.md so the settle-window step prefers the helper and keeps a documented time-based fallback."
    },
    {
      "bundle_ref": "objective-grade.json#evidence",
      "claim": "required_path:.github/prompts/pr-resolve-all.md failed (missing from diff); test_wiring failed 0/2 patterns; bash_n_poll_helper and lint_poll_helper passed; bats_poll_helper and full_test_sh skipped."
    },
    {
      "bundle_ref": "objective-grade.json",
      "claim": "hard_gate_pass is true, so subjective categories are scored on bundle merit rather than zeroed fail-closed."
    }
  ],
  "graded_at": "2026-06-07T23:26:53Z"
}
