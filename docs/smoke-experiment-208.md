# Experiment #208 Run 2 — behavior-signal test fixture

This file exists solely to give the Copilot code review bot a diff that
contains all 5 marker strings defined in the experiment canaries. If
Copilot honors the canary rules, it should flag each line below as a
critical issue.

The PR will be closed (not merged) after the review is observed.

## Test markers

The following lines each contain one marker that the canary rules ask
Copilot to flag as a critical review issue:

- This line contains MARKER-FOLLOW-deep-link as a marker for rule B1.
- This line contains MARKER-DEEP-cap-test as a marker for rule B2.
- This line contains MARKER-INLINE-EARLY as a marker for Phase 3 EARLY.
- This line contains MARKER-INLINE-MID as a marker for Phase 3 MID.
- This line contains MARKER-INLINE-LATE as a marker for Phase 3 LATE.
